const foo = "bar";

const obj = {
  x: 100,
  y: 32,
  foo,
  complex: {
    foo: "'foo'",
    bar: '"bar"',
  },
};

print(foo, (45/2) * 3);

const timeNow = time();

print(timeNow);

function makeAdder(offset){

  function add (x,y) {
    x + y + offset;
  }

  add
}


const adder = makeAdder(1);

print(adder(10,5));

function sub() {
  function add (x,y) {
    let result = x + y;
    print(result);

    result; 
  }

  let foo = 45;
}


print(-5 + 5);

if(!(obj.x == 100)) {
  print("X is one hundred.");
} else if(obj.y == 32) {
  print("Y is thirty two.");
} else {
  print("something is not right!")
}


if(obj.x == 100) {print("It's true");}

function test_while_loop(i){
  while(i < 1000) : (i = i + 1) {
    if(i==2) continue;
    else if(i==5) break;
    print(i);
  }

  return "While loop works!";
}

print(test_while_loop(1));

print(obj["foo"]);