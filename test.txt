const foo = "bar";

const obj = {
  x: 100,
  y: 32,
  foo,
  complex: {
    foo: "'foo'",
    bar: '"bar"',
  },
};

print(foo, (45/2) * 3);

const timeNow = time();

print(timeNow);

function makeAdder(offset){

  function add (x,y) {
    x + y + offset;
  }

  add
}


const adder = makeAdder(1);

print(adder(10,5));

function sub() {
  function add (x,y) {
    let result = x + y;
    print(result);

    result; 
  }

  let foo = 45;
}