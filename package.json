{
  "name": "zigscript",
  "private": true,
  "scripts": {
    "build": "bun build src/main.ts --outdir=dist --target=netlify",
    "start": "bun run src/main.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
